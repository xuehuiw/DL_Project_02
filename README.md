# 🎗️ Breast Cancer Diagnosis via Deep Learning

![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![Keras](https://img.shields.io/badge/Keras-2.0%2B-red)
![License](https://img.shields.io/badge/License-MIT-green)

本项目利用全连接神经网络（DNN）对乳腺癌数据集进行二分类（良性 Benign / 恶性 Malignant）。项目包含完整的数据预处理、模型构建、训练可视化及评估流程，适合深度学习初学者作为入门实践参考。

---

## 📋 目录 (Table of Contents)
- [背景](#背景)
- [核心特性](#核心特性)
- [目录结构](#目录结构)
- [环境安装](#环境安装)
- [快速开始](#快速开始)
- [模型表现](#模型表现)
- [代码详解](#代码详解)

## 📖 背景
乳腺癌的早期诊断对于提高治愈率至关重要。本项目旨在利用机器学习/深度学习技术，通过分析细胞核特征数据，辅助医生进行良恶性判断。

## ✨ 核心特性
- **数据处理**：包含 MinMax 归一化、One-Hot 编码处理。
- **深度模型**：基于 Keras 搭建多层感知机（MLP/DNN）。
- **可视化**：自动绘制 Loss 和 Accuracy 训练曲线。
- **评估报告**：输出详细的 Precision, Recall, F1-Score 报告。

## 📂 目录结构
```text
DL_Project_02/
├── data/
│   └── breast_cancer_data.csv  # [核心数据] 乳腺癌特征数据集
├── scr/
│   ├── model_train.py          # [训练脚本] 数据加载 -> 模型训练 -> 保存(.h5)
│   └── model_test.py           # [测试脚本] 加载模型 -> 预测 -> 评估报告
├── output/                     # [输出文件夹] 
│   ├── model.h5                # [模型权重] 训练产生的模型文件
│   └── README.txt              # 输出说明
├── requirements.txt            # [依赖文件] 项目所需 Python 库
└── README.md                   # 项目文档
```

## ⚙️ 环境安装

确保本地已安装 Python 3.x 环境。建议按以下步骤安装依赖：

```bash
# 1. 克隆项目
git clone [repository_url]

# 2. 安装依赖库 (通过 requirements.txt)
pip install -r requirements.txt
```

## 🚀 快速开始

### 1. 训练模型
运行训练脚本，模型将自动保存到 `output/model.h5`：
```bash
python scr/model_train.py
```
> **输出**：控制台显示训练进度，并在结束时弹出 Loss/Accuracy 趋势图。

### 2. 模型评估
运行测试脚本，加载已保存的模型进行预测：
```bash
python scr/model_test.py
```
> **输出**：控制台打印分类报告（Classification Report），包含精确率与召回率。

## 📊 模型表现 (Results)
*   **架构**：Input(30) -> Dense(10, relu) -> Dense(10, relu) -> Output(2, softmax)
*   **优化器**：SGD
*   **损失函数**：Categorical Crossentropy

*(此处可补充你实际运行出来的准确率截图或数据，例如：Test Accuracy: 96%)*

## 📝 代码详解 (小白版)

### A. 训练篇 (`model_train.py`)
这份代码的目的是**“教”**电脑如何根据数据判断乳腺癌是良性还是恶性。

**第一步：搬运工具箱 (导入库)**
```python
import numpy as np                  # 数学计算工具，处理数字很厉害
import pandas as pd                 # 表格处理工具，像Python里的Excel
from sklearn.preprocessing import MinMaxScaler  # 数据美容师：用来把数据缩放到0-1之间
from sklearn.model_selection import train_test_split # 切蛋糕工具：把数据分成训练用和考试用
import keras                        # 深度学习框架，核心大脑
from keras.layers import Dense      # 神经层：全连接层
from keras.utils import to_categorical # 翻译官：把类别数字变成只有0和1的向量
```

**第二步：读取与处理数据**
1.  **读数据**：`pd.read_csv("data\\breast_cancer_data.csv")` 就像把 Excel 表格读进内存。
2.  **分特征和答案**：
    *   `dataset.iloc[:, :-1]`：拿走除最后一列外的所有数据作为题目。
    *   `dataset['target']`：拿走最后一列作为标准答案。
3.  **切分训练集和测试集**：`train_test_split(..., test_size=0.2)` 留20%的数据作为期末考试题，不给模型看。
4.  **数据整形 (One-Hot编码)**：`to_categorical` 把 `0` (良性) / `1` (恶性) 变成 `[1, 0]` / `[0, 1]` 这种机器喜欢的格式。
5.  **归一化 (MinMaxScaler)**：
    ```python
    sc = MinMaxScaler(feature_range=(0, 1))
    x_train = sc.fit_transform(x_train) # 算出规则并转换训练数据
    ```
    *   把所有数据压缩到 0-1 之间，防止大数值特征（如面积）抢了小数值特征（如光滑度）的风头。

**第三步：搭建“大脑” (构建模型)**
```python
model = keras.Sequential()  # 建一个空壳子
model.add(Dense(10, activation='relu'))   # 第1层：10个神经元
model.add(Dense(10, activation='relu'))   # 第2层：10个神经元
model.add(Dense(2, activation='softmax')) # 输出层：2个神经元(良性/恶性)
```

**第四步：教学与保存**
1.  `model.compile(...)`：配置学习规则（惩罚机制 loss 和 改错方法 optimizer）。
2.  `model.fit(...)`：开始特训！刷110遍题 (epochs)。
3.  `model.save('output/model.h5')`：学成出师，把“脑子”存下来。

---

### B. 测试篇 (`model_test.py`)
这份代码的目的是**“用”**训练好的模型去考试。

**关键点：数据处理的一致性**
```python
# 将数据特征进行归一化
sc = MinMaxScaler(feature_range=(0, 1))
sc.fit(x_train)  # <--- 必须用训练集的数据来定标准！
x_test = sc.transform(x_test)
```
*   **解释**：我们不能用考试题的标准来衡量平时练习，必须用平时练习的标准（`x_train`的分布）来衡量考试题。

**核心流程**
1.  **唤醒模型**：`model = load_model("output/model.h5")` 读取之前保存的文件。
2.  **考试 (预测)**：`predict = model.predict(x_test)` 让模型做题。
    *   模型输出的是概率，比如 `[0.9, 0.1]`。
3.  **翻译结果**：
    *   `np.argmax`：找出概率最大的那个位置（0或1）。
    *   通过循环把 `0` 翻译成“良性”，`1` 翻译成“恶性”。
4.  **最终阅卷**：
    *   `classification_report(...)` 打印出精确率（Precision）和召回率（Recall）。

---
**Maintainer**: 18261